{
  "name": "Voting App for Free Code Camp challenge",
  "version": "1.0.0",
  "description": "As part of the Free Code Camp challenges this is the Voting App",
  "scripts": {
    "build:webpack": "NODE_ENV=production webpack --config webpack.config.prod.js",
    "build": "npm run clean && npm run build:webpack",
    "test": "NODE_ENV=production mocha './tests/**/*.spec.js' --compilers js:babel-core/register",
    "clean": "rimraf dist",
    "start": "node devServer.js",
    "tunnel": "browser-sync start --proxy localhost:7770 --tunnel aevans"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/alex-evans/Voting-App.git"
  },
  "author": "Alex Evans",
  "license": "MIT",
  "homepage": "",
  "dependencies": {
  },
  "devDependencies": {
  }
}
